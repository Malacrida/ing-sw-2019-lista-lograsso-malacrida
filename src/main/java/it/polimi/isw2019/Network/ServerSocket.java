package it.polimi.isw2019.Network;

class Socket {

    /*private final int serverPort;
    private ServerSocket serverSocket;

    public Socket(serverPort){
        this.serverPort = serverPort;
        try {
            this.serverSocket = new ServerSocket(serverPort);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }*/



}
